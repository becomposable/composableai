{
    "name": "@becomposable/studio",
    "version": "0.40.0",
    "private": true,
    "scripts": {
        "bump": "pnpm exec wst bump minor -x llumiverse",
        "release": "pnpm -r publish --access public",
        "build": "pnpm -r build",
        "test": "pnpm -r test",
        "clean": "rimraf ./node_modules && pnpm -r clean"
    },
    "devDependencies": {
        "cross-spawn": "^7.0.5",
        "npm-ws-tools": "^0.3.0",
        "rimraf": "^5.0.5",
        "typescript": "^5.3.3"
    },
    "packageManager": "pnpm@9.13.2"
}
