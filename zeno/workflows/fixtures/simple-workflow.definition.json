{
    "name": "extract text and set status",
    "tags": ["test"],
    "description": "extract text from a pdf and set the status of the document",

    

    "activities": [
        {
            "name": "extractText",
            "output": "extracted"
        },
        {
            "name": "setObjectStatus",
            "output": "status",
            "params": {
                "objectId": "${params.objectIds[0]}"
            }
        }
    ]
}